---
description: 作業・コミットは常に main で行う
alwaysApply: true
---

# ブランチワークフロー

- **作業ブランチは `main` のみ**。機能追加・修正・コミット・PR はすべて `main` で行う。
- 別ブランチにいる場合は、作業前に `git checkout main` する。
- **`chromebook` では開発しない**（レガシー同期先。新規作業の起点にしない）。
- リモートの既定ブランチが `chromebook` でも、ローカルの作業起点は `main` のままにする。
