# Placeholder — replace with the output from your machine before rebuilding:
#
#   sudo nixos-generate-config --show-hardware-config > hosts/mitac/hardware-configuration.nix
#
# Applying this stub as-is will fail because real fileSystems / boot settings are missing.
{
  imports = [ ];

  # Intentionally empty. Copy the generated hardware-configuration.nix from the target host.
}
